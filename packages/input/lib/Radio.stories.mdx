import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks'
import { Radio } from './input'
import withDesignTokens from 'addons/tokens/index.js'
import { PeerDependencies } from 'addons/packages/index.js'
import { FlexContainer } from 'addons/utils/blocks.js'
import changelog from '../CHANGELOG.md'
import packageJson from '../package.json'

<Meta
  component={Radio}
  decorators={[withDesignTokens]}
  parameters={{ changelog }}
  title="Inputs/Radio Input"
/>

# Radio
### Installation

`npm install @kyper/input --save`

<PeerDependencies json={packageJson} />

<Preview>
  <Story name="Basic">
    <FlexContainer direction="column">
      <FlexContainer padding={16}>
        <Radio id="g1-1" label="Option One Label Right" labelPosition="right" name="group1" />
        <Radio id="g1-2" label="Option Two Label Right" labelPosition="right" name="group1" />
      </FlexContainer>
      <FlexContainer padding={16}>
        <Radio id="g2-1" label="Option One Label Left" name="group2" />
        <Radio id="g2-2" label="Option Two Label Left" name="group2" />
      </FlexContainer>
      <FlexContainer padding={16}>
        <Radio id="g3-1" disabled={true} label="Disabled Left" name="group3" />
        <Radio id="g3-2" checked={true} disabled={true} label="Disabled right" name="group3" labelPosition="right"/>
      </FlexContainer>
    </FlexContainer>
  </Story>
</Preview>

### Props
Any additional props will be passed down to the native input element.

<Props of={Radio} />

