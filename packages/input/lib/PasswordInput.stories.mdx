import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks'
import { PasswordInput, TextInput } from './input'
import withDesignTokens from 'addons/tokens/index.js'
import { PeerDependencies } from 'addons/packages/index.js'
import { FlexContainer } from 'addons/utils/blocks.js'
import changelog from '../CHANGELOG.md'
import packageJson from '../package.json'

<Meta
  component={PasswordInput}
  decorators={[withDesignTokens]}
  parameters={{ changelog }}
  title="Inputs/Password Input"
/>

# PasswordInput
### Installation

`npm install @kyper/input --save`

<PeerDependencies json={packageJson} />

<Preview>
  <Story name="Basic">
    <FlexContainer direction="column">
      <FlexContainer padding={16}>
        <PasswordInput label="Password" name="password" showSecondaryLabelBackground={true} />
        <PasswordInput label="With Toggle" name="password2" allowToggle={true} showSecondaryLabelBackground={true} error="test" />
        <PasswordInput label="Custom aria labels" name="password3" allowToggle={true} ariaShowLabel="Montrer le mot de passe" ariaHideLabel="Masquer le mot de passe" />
      </FlexContainer>
    </FlexContainer>
  </Story>
</Preview>

### Unique Props
<Props of={PasswordInput} />

### Inherited Props
The following props are from the `TextInput` component
<Props of={TextInput} />
